describe("First Test", () => {
  it("does what is supposed to", () => {
    expect(1).toBe(1);
  });
});

describe("Another test", () => {
  it("Should do another", () => {
    expect(3).toBe(3);
  });
});
